# ============================================
# LOCAL DEVELOPMENT ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env.local for local development
# NEVER commit .env.local to git!
#
# For production: All secrets are fetched from Google Cloud Secret Manager
# ============================================

# Google Cloud Configuration (for Secret Manager access)
# Option 1: Use gcloud CLI authentication (recommended)
#   Run: gcloud auth application-default login
#
# Option 2: Use service account key file
#   GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
#
# Option 3: Specify project ID explicitly
GOOGLE_CLOUD_PROJECT=careful-drummer-468512-p0

# ============================================
# PUBLIC CONFIGURATION (Safe to expose)
# ============================================

# Sanity Configuration
NEXT_PUBLIC_SANITY_PROJECT_ID=svg08vjt
NEXT_PUBLIC_SANITY_DATASET=production

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://cibjeqgftuxuezarjsdl.supabase.co

# Site Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_AUTHORIZED_EMAIL=your-email@example.com

# ============================================
# SENSITIVE SECRETS (For local dev only)
# ============================================
# In production, these are fetched from Google Cloud Secret Manager
# Only add values here for local development if Secret Manager is not configured

# Sanity API Token (get from sanity.io/manage)
# SANITY_API_TOKEN_STG=your-dev-token-here

# Supabase Service Key (get from Supabase dashboard)
# SUPABASE_SERVICE_KEY_STG=your-dev-key-here

# Supabase Anon Key (public client key)
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Unsplash API Key (get from unsplash.com/developers)
# UNSPLASH_ACCESS_KEY=your-dev-key-here

# ============================================
# NOTES
# ============================================
# 1. For local development without Secret Manager access, uncomment and fill in the secrets above
# 2. For production/staging, ensure secrets are created in Google Cloud Secret Manager
# 3. Secret naming convention: {SERVICE}_{KEY_TYPE}_{ENVIRONMENT}
#    Examples: SANITY_API_TOKEN_STG, SANITY_API_TOKEN_PROD
# 4. See SECRETS.md for full documentation on secret management